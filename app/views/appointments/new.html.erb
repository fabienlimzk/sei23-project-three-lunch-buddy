<h1 class="text-center">Looking for a lunch buddy?</h1>
<div class="card mx-auto col-7 shadow p-3 mb-5 bg-white rounded text-center">
<% if @appointment.errors.any? %>
  <%= pluralize(@appointment.errors.count, "error") %>
    <% @appointment.errors.full_messages.each do |message| %>
      <div class="alert alert-danger"> <%= message %> </div>
    <% end %>
<% end %>

<h4 class="text-left">Create a new lunch appointment</h4>
<%= form_for [@user, @appointment] do |f| %>
  <div class=“form-row”>
    <%# f.label :content, "Content" %>
    <%= f.text_area :content, placeholder: "Looking for a lunch buddy around MBFC or Marina One area :)", :rows => 4, style: 'width:100%;' %>
  </div>
  <div class="form-row mt-3">
    <%= f.collection_select(:location_selects, Location.all, :name, :name, {:prompt => "Choose Location(s)"},{:required => true, :class => "custom-select"}) %>
  </div>

  <div class="form-group text-left mt-3">
    <%= f.label :status, "Status" %>
    <%= f.text_field :status, class: "form-control" %>
  </div>

  <% if @user.isAdmin %>
    <div class="form-group mt-3 text-left">
      <%= f.label :location_list, "Add a new location" %>
      <%= f.text_field :location_list, class: "form-control" %>
    </div>
  <% end %>


  <div class=“form-row”>
    <%= f.submit "Create appointment", class: "btn btn-success mt-3" %>
  </div>
<% end %>
</div>
