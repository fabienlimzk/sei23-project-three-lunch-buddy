<div class="row d-flex flex-wrap">
<% @cuisines.each do |cuisine| %>
  <div class="border col-3 p-2 mx-1 text-center">
    <%= link_to cuisine.name, cuisine_path(cuisine) %>
  <% if current_user.isAdmin %>
    <div class="d-block">
      <%= link_to "Edit", edit_cuisine_path(cuisine), class: "btn btn-warning" %>
      <%= link_to "Delete", cuisine_path(cuisine), method: "delete", data:{confirm: "Delete this cuisine?"}, class: "btn btn-danger" %>
    </div>
   <% end %>
  </div>
<% end %>
</div>
